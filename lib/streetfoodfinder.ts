// StreetFoodFinder's site is behind Cloudflare bot protection —
// server-side fetching is not possible. The /find-us page embeds
// their live tracker as an iframe (which works in real browsers).
// The homepage schedule cards are driven by lib/schedule.ts instead.
export {};
